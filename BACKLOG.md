# BACKLOG — L'Encre Humaine

Suivi de progression du site (spec-driven). Source de vérité : `docs/`.
Légende : `[ ]` à faire · `[~]` en cours · `[x]` fait · `[!]` bloqué.

> **Convention** : on n'écrit pas de code sans spec. Chaque item référence sa spec.
> DoD par feature : spec respectée + critères d'acceptation ✅ · Biome/typecheck 0 warning ·
> Vitest + Playwright verts · Lighthouse ≥ 95 mobile · WCAG AA · env validé au boot.

---

## Décisions actées (ce projet)

- **UI** : Reka UI (primitives headless) + Tailwind. Pas de lib JS lourde côté public.
- **Validation** : `valibot` (léger), schémas dans `packages/shared`, réutilisés client + serveur.
- **Docs** : les specs vivent dans `docs/` (l'amorce mentionne `specs/` — on reste sur `docs/`).
- **Git** : dépôt `main`, hébergement Forgejo, miroir GitHub (à câbler).
- **Palette (rebrand « marine / doré / crème »)** : identité = **marine `#1C2B4A`** (fonds sombres
  + organisations) + **doré `#C9A84C`** (accents : soulignés, filigranes poulpe, « pop ») +
  **crème `#F5F2EB`** (fonds clairs). Implémenté **par repointage des tokens** dans
  `apps/web/assets/css/main.css` (un seul fichier → bascule instantanée, retour arrière =
  `git checkout`). ⚠️ **Dette assumée** : les *noms* d'échelles Tailwind restent `teal-*` (= marine),
  `orange-*`/`sand-*` (= doré) pour éviter de toucher ~230 usages. Doré vif sur `*-300/400`
  (accents), doré antique plus profond sur `*-500/600/700` (aplats : passe l'AA avec texte blanc,
  donc aucun bouton à réécrire).
  - [ ] **Si le rebrand reste** : renommer les classes `teal→navy`, `orange/sand→gold` (gros sweep, d'un coup).
  - **Ancienne palette (revert/comparaison)** : ink `#062b28`/`#04211f`/`#0a3a36` ·
    paper `#fbf6ee`/`#f4ebdc`/`#efe3d0` · teal `#0f675e`(700) `#169c89`(500) `#6dd3c2`(300) ·
    orange `#f25c2a`(500) `#fb7a4a`(400) · sand `#f6c14a`(400) `#fbd77a`(300).

## Points ouverts / à fournir par Franck

- [ ] Contenu CGV (prestations + produits physiques, rétractation 14 j) — `docs/10-legal.md`.
- [ ] Identité légale : `legal_name`, `legal_status`, `siret`, adresse, hébergeur — pour `site_settings`.
- [ ] Secrets réels (Stripe, Resend, Turnstile, Cloudflare, R2, Directus) au moment du déploiement.
- [x] ~~Docker indisponible dans ce WSL~~ → **résolu** (Docker Desktop était éteint ; server 29.5.2, compose v5.1.4).
- [ ] URL Forgejo + remote GitHub miroir.

---

## Phase 0 — Socle technique (run en cours)

### Scaffold monorepo & outillage — `docs/CLAUDE.md` §Méthode 1
- [x] `git init` (main) + `.gitignore` + `.node-version` + `.editorconfig`
- [x] `pnpm-workspace.yaml` + `package.json` racine (scripts) + `tsconfig.base.json`
- [x] Biome (lint + format) — `biome.json`
- [x] `renovate.json` (`minimumReleaseAge: 30 days`, vulnérabilités prioritaires)
- [x] `Makefile` (raccourcis dev) + `infra/docker-compose.dev.yml` (overlay dev opt-in, Postgres exposé sur `127.0.0.1:55432`) : `make db-up` · `psql` · `psql-app` · `db-migrate` · `query` · `db-reset`
- [x] Vitest (par package : `vitest.config.ts` shared/db en env Node, `apps/web` en env Nuxt via `@nuxt/test-utils` + happy-dom ; orchestration racine `pnpm test` = `pnpm -r` — chaque projet tourne dans son cwd, requis par la config Nuxt liée au cwd). Vitest unifié sur **4.1.6** (peer de `@nuxt/test-utils` 4.0.3). **8 tests de composants** : `ConsentBanner` (affichage/accept/refus + valeur `thirdParty`), `NavMobile` (déclencheur labellisé, dialog titré + nav, contenu téléporté), `ContactForm` (validation valibot client, honeypot → succès silencieux). Total **23 tests** verts (shared 10, db 5, web 8)
- [~] Playwright (parcours critiques) — **amorce faite** : `apps/web/playwright.config.ts` + smoke `e2e/health.spec.ts` (`GET /api/health` → `{status:"ok"}`, requête API pure, sans navigateur). `webServer` lance `nuxi dev` avec un env SYNTHÉTIQUE valide (passe la validation au boot, aucun service tiers contacté → CI-runnable sans secret). Les vrais parcours (accueil/boutique/contact/newsletter) = Phase 1 quand les pages existeront
- [x] Validation d'env au boot (`packages/shared`, valibot) — `docs/06-security.md` §5 — plugin Nitro `00.validate-env.ts`, **vérifié** (web refuse de démarrer si var manquante/malformée)

### `packages/shared` — schémas & types partagés
- [x] Schéma d'env (valibot) + parse au boot (`env.ts` + test)
- [x] Enveloppe d'erreur uniforme `{ error: { code, message } }` (`errors.ts` : `ErrorCode`, `errorEnvelope`, `STATUS_BY_CODE`)
- [x] Schémas de validation : contact, newsletter subscribe — `docs/03-api-contracts.md` (réutilisés par les forms Nuxt)

### `packages/db` — Drizzle (schéma `app`) — `docs/01-data-model.md`
- [x] `_schema.ts` (pgSchema app) + `_helpers.ts` (pk uuidv7, timestamps) + `enums.ts`
- [x] Tables `orders`, `contact_leads`, `newsletter_subscribers`
- [x] Exports de types dérivés (`InferSelectModel`/`InferInsertModel`)
- [x] `drizzle.config.ts` + génération migration initiale (`0000_messy_yellowjacket.sql`, `CREATE SCHEMA IF NOT EXISTS app` pour coexister avec `init.sql`)
- [~] Test Vitest : idempotence `orders.stripe_session_id` — test structurel (index unique) ✅ ; comportement `ON CONFLICT DO NOTHING` (double insert → 1 ligne) **vérifié manuellement contre Postgres 17 réel** le 2026-06-08 ; test automatisé du comportement = avec l'endpoint webhook (Phase 1)

### `packages/directus` — schéma + accès — `docs/02-content-model.md`
- [x] Bootstrap idempotent (`bootstrap.ts` : DSL `fields.ts` → `schema.ts` → moteur) — **vérifié contre Directus 11.12 réel** : 16 collections (8 singletons + 8) + jonction `products_files`, bloc statut/SEO, re-run = +0 partout
- [x] Scripts `snapshot` / `apply` (`make cms-snapshot` / `cms-apply`) — round-trip prouvé (snapshot → apply = « No changes to apply »). Snapshot versionné : `packages/directus/snapshots/schema.yaml`. Le CLI Directus s'invoque via `node cli.js` (pas dans le PATH de l'image)
- [x] Rôles : Admin (système, Franck), Éditrice (Eléonore, CRUD contenu+fichiers : 72 perms), API lecture seule published-only (18 perms) + user à token statique — vérifié actif
- [x] Client Directus typé (`@directus/sdk`) côté serveur Nuxt → cf. Phase 1

### `infra` — `docs/07-deploy.md`
- [x] `.env.example` (toutes les variables) + schéma de validation (commentaires hors-valeur pour `env_file`)
- [x] `docker-compose.yml` (caddy · web · directus · postgres · umami · migrate) — toutes les images buildables
- [x] `Caddyfile` (3 sous-domaines, en-têtes sécurité + CSP) — validé/formaté (cf. image Caddy custom)
- [x] Image Caddy custom (`infra/caddy/Dockerfile`, Caddy 2.10.2 + module `caddy-dns/cloudflare` via xcaddy, TLS DNS-01) — **buildée & vérifiée** (image 144 MB, `dns.providers.cloudflare` présent, `Caddyfile` validé/formaté). TLS réel = au déploiement (token Cloudflare + DNS)
- [x] `postgres/init.sql` (schémas `app`/`directus`/`umami` + rôles séparés) — **validé contre Postgres 17 réel** (3 rôles + 3 schémas, `app_user` n'a pas accès à `directus`). Bug corrigé : psql n'interpole pas `:'pw'` dans un bloc `$$` → passage par `set_config`/`current_setting` + `EXECUTE format(%L)`
- [x] Dockerfile `web` (`apps/web/Dockerfile`, multi-stage, Nitro node-server, **non-root** `uid 1000`) + `.dockerignore` racine — **buildé & vérifié** (image 402 MB, `/api/health` ok, healthcheck Docker `healthy` via `fetch` natif Node 26, pas de wget/corepack dans l'image slim)
- [x] Job migrate avant rollout web (service one-shot `migrate`, cible Docker partagée) — applique drizzle-kit **en superuser** (DDL : `app_user` ne peut pas `CREATE SCHEMA`), `web` attend `service_completed_successfully`. **Vérifié** : 3 tables créées (owner postgres, `app_user` reçoit ALL via `ALTER DEFAULT PRIVILEGES`), 2ᵉ run idempotent. Bug latent corrigé : commentaires inline dans `.env.example` embarqués dans la valeur par `env_file` → sortis sur leur propre ligne
- [x] Backups `pg_dump` → R2 (rclone) + test de restauration — `infra/backup/` : image `postgres:17.6-alpine` + rclone/gnupg/coreutils, conteneur **cron** (`backup.sh` : `pg_dump -Fc | gpg AES256 | rclone → R2`, rétention **GFS-lite** 7 quotidiens / 4 hebdo), `restore.sh` symétrique (`pg_restore --clean`). Remote rclone **100 % par variables d'env** (aucun fichier de conf ; surchargeable `local` pour test). Service `backup` dans le compose + cibles Make `backup-build`/`backup-run`/`restore` + vars `.env.example` + README. **Restauration testée de bout en bout** (`infra/backup/test-restore.sh`, contre Docker réel) : dump→gpg→rclone→cluster cible vierge `init.sql`-isé→sentinelle restaurée, `app_user` relit sa table (owners/GRANTs préservés). S3/R2 réel (endpoint/clés) = étape déploiement (cf. bloqueurs secrets)

### `apps/web` — Nuxt + Nitro (squelette)
- [x] `nuxt.config` + modules (seo, image, fonts, tailwind v4, reka-ui). **Fix (2026-06-17)** : les secrets **serveur** de `runtimeConfig` (`directusReadToken`, `stripeSecretKey`, `appDatabaseUrl`, `stripe*`, `resend*`, `newsletterFrom`, `contactNotifyTo`, `turnstileSecretKey`) étaient à `""` sans pont `process.env` → Nuxt n'override que les clés `NUXT_*`, donc ces secrets restaient vides au runtime (les utils auraient levé « non configuré » **même en prod** avec `env_file` en noms simples). Pontés via `process.env.X ?? ""` (symétrique au bloc `public`), vérifié en live.
- [x] Layout global + `app.vue` + thème teal/orange — `docs/00-global.md`
- [x] Composants d'inventaire (docs/00-global.md §Composants) — **14 composants + 2 composables + types** : `SectionHeading`, `CtaBlock`, `StatRow`, `TestimonialCard`, `OfferCard`, `ArticleCard`, `ProductCard`, `RichText` (présentationnels, masquage si vide) ; `NavMobile` (Reka Dialog : focus trap/Escape/ARIA), `FaqAccordion` (Reka Accordion), `ConsentBanner` (gate tiers), `CalendlyEmbed` (load au consentement), `ContactForm`/`NewsletterForm` (valibot partagé + Turnstile + honeypot + états). Composables `useConsent` (cookie) / `useTurnstile`. Module `reka-ui/nuxt` activé. NavMobile branché dans `AppHeader`, ConsentBanner dans le layout. Lint + typecheck + build verts. Câblage du contenu Directus + tests = phases suivantes
- [x] Validation d'env au boot (refus de démarrer si var manquante)

---

## Phase 1 — Pages & features (après socle)

### Données & contenu
- [x] Migration initiale Drizzle appliquée (schéma `app`) — appliquée sur le volume dev + **vérifiée via le job `migrate` dockerisé** (idempotent)
- [x] Collections Directus créées + snapshot versionné + rôles (cf. `packages/directus`, Phase 0)
- [x] Client Directus typé (`@directus/sdk`) côté serveur Nuxt (token lecture seule) — **codegen depuis le snapshot** : `pnpm directus:types` (`make cms-types`) lit `snapshots/schema.yaml` → génère `packages/directus/src/schema.gen.ts` (interface `Schema` + un type par collection, relations résolues m2o/o2m/files, singletons vs tableaux). **Types dérivés, jamais ré-écrits** (DRY, docs/02 §Accès), codegen **reproductible**. Client serveur `apps/web/server/utils/directus.ts` : `createDirectus<Schema>().with(staticToken).with(rest())`, token *published-only* strictement serveur, mémoïsé. Lint + typecheck + tests verts. Câblage du contenu dans les pages = items pages ci-dessous

### Endpoints Nitro — `docs/03-api-contracts.md`
- [x] `POST /api/stripe/webhook` (raw body, signature, idempotence ON CONFLICT) + **test Vitest idempotence** — handler `apps/web/server/api/stripe/webhook.post.ts` (body brut via `readRawBody`, `constructEvent` → 400 si signature KO, `checkout.session.completed`/`charge.refunded`, 2xx après persistance). Logique « dure » sans globals Nitro dans `server/utils/orders.ts` (`sessionToOrder` + `recordCheckoutSession` ON CONFLICT DO NOTHING + emails best-effort + `applyRefund`). Utils `db.ts`/`stripe.ts`/`resend.ts`/`api-error.ts`. **Idempotence prouvée sous PGlite** (`test/stripe-webhook.spec.ts`, env node) : 2× même session → 1 commande + 1 jeu d'emails ; échec email ≠ échec persistance. DRY : opérateurs drizzle ré-exportés depuis `@encre/db` (instance unique)
- [x] `POST /api/contact` (valibot + Turnstile serveur + rate-limit + Resend) — `apps/web/server/api/contact.post.ts` : honeypot `website` → validation `ContactPayloadSchema` (partagée) → `verifyTurnstile` (fail-closed) → `enforceRateLimit` 5/min/IP → INSERT `contact_leads` → `sendContactNotification` best-effort (`notification_sent=true` si OK). Utils partagés `client-ip.ts` (CF-Connecting-IP), `rate-limit.ts` (fenêtre glissante en mémoire, **testé** `test/rate-limit.spec.ts`), `turnstile.ts` (siteverify). Aucune IP persistée.
- [x] `POST /api/newsletter/subscribe` (double opt-in, token hashé) — `server/api/newsletter/subscribe.post.ts` : honeypot → validation → Turnstile → rate-limit 5/min/IP → UPSERT `pending` (token 32o base64url, **seul le hash SHA-256 stocké**, `expires_at`=now+30j, consent_ip/UA) → contact Resend en **UNSUBSCRIBED** (`upsertNewsletterContact`) → email de confirmation tokenisé. `confirmed` existant → 200 `already_subscribed` sans mail ; `pending`/`unsubscribed` → régénère + repousse. Primitives crypto dans `server/utils/newsletter.ts`
- [x] `GET /api/newsletter/confirm` (comparaison constante, expiration) — `server/api/newsletter/confirm.get.ts` : `tokenMatchesHash` (timingSafeEqual), vérifie expiration, passe `confirmed`+efface le token, contact Resend → SUBSCRIBED, **redirige 302** vers `/newsletter/confirmation?status=success|expired|invalid|already`. ⚠️ déviation docs/03 §3.1 : le lien email pointe sur l'endpoint API (qui redirige) plutôt que directement la page — à valider
- [x] Purge planifiée des `pending > 30j` (DB + Resend) — tâche Nitro `server/tasks/newsletter/purge.ts` (`defineTask`, programmée `nitro.scheduledTasks` quotidien 03h) → `purgeExpiredPending` (supprime contact Resend puis ligne DB, conserve les `confirmed`). **Testé sous PGlite** + primitives crypto testées (`test/newsletter.spec.ts`)
- [x] `GET /api/shop/products` (merge Directus + Stripe, cache court) — `server/api/shop/products.get.ts` (`defineCachedEventHandler` maxAge 60s) → `loadCatalog` (`server/utils/shop.ts`) : lit les produits Directus published, liste les prix Stripe actifs (`expand product`), merge via `buildPriceMap`+`mergeCatalog` (purs, **testés** `test/shop.spec.ts`) — n'expose que les produits actifs des deux côtés, images résolues en URLs d'assets, **jamais de prix en dur**
- [x] `POST /api/shop/checkout` (Checkout Session, FR, port forfait, 293 B) — `server/api/shop/checkout.post.ts` : rate-limit 10/min/IP → validation `CheckoutPayloadSchema` (partagée, **seul le priceId circule**) → revalidation des priceId contre le catalogue actif (produit dépublié/inactif ⇒ non achetable) → fusion des quantités → `checkout.sessions.create` (mode payment, locale fr, `shipping_address_collection` FR, `shipping_rate` forfait, `automatic_tax` piloté par `VAT_ENABLED`, mention 293 B en franchise, `invoice_creation`) → renvoie `url`

### Pages — `docs/01..10`
- [x] `/` Accueil — `docs/01-accueil.md` — page branchée sur `home_page` + 3 derniers `articles` publiés + témoignage vedette, via l'endpoint serveur caché `GET /api/content/home` (`defineCachedEventHandler` 60s, token Directus strictement serveur). Loader `server/utils/content/home.ts` : mappers **purs** (coercion des répéteurs JSON `stats`/`tags`, résolution des URLs d'assets, SEO avec fallback `site_settings`, masquage propre des sections vides — témoignage/articles/blocs). **11 tests Vitest** (`test/home.spec.ts`, 18 `it`) verts. SEO via `useSeoMeta` (titre/desc/OG/robots `no_index`), a11y (h1=hero, CTA = vrais liens, alt). Champs fichier = ID brut (directus_files hors Schema typé, cf. `shop.ts`). ⚠️ e2e Playwright réel = différé (nécessite une fixture Directus, cf. note Playwright Phase 0)
- [x] `/a-propos` — `docs/02-a-propos.md` — page branchée sur `about_page` via l'endpoint caché `GET /api/content/about` (loader `server/utils/content/about.ts`). Les **4 champs `input-rich-text-html`** (`story_body`, `why_body`, `octopus_body`, `what_i_dont_do`) sont **assainis côté serveur** au fetch (`server/utils/sanitize.ts`, **`sanitize-html` 2.17.5** pinné, allowlist alignée sur `.rich-text`) — sanitizer **injecté** dans le mapper pur (testable). 9 sections, 2 colonnes desktop / empilé mobile, masquage propre des vides, `RichText` pour le HTML. **Helpers communs extraits** dans `server/utils/content/_shared.ts` (résolution fichier→asset, `mapSeo`, `mapStringList`) — DRY, réutilisés par tous les loaders ; `home.ts` refactoré dessus (page Accueil revérifiée verte). **Tests** : `about.spec.ts` (8), `sanitize.spec.ts` (6, retire script/handlers/`javascript:`, durcit liens), `content-shared.spec.ts` (helpers). **58 tests web verts**, lint/typecheck/build (EXIT 0, endpoints émis) verts
- [x] `/organisations` Hub B2B — `docs/03-organisations-hub.md` — page branchée sur `org_hub_page` + `offers` (audience=organisation, **cartes dynamiques** triées par `sort`) + `testimonials` (b2b, masqués si vides), via l'endpoint caché `GET /api/content/org-hub`. 6 sections (accroche h1, offres `OfferCard`, méthode en étapes, « pour qui », témoignages, CTA). **La page oriente, ne détaille pas** : seul le résumé d'offre est lu (titre/slug/phrase/durée/tarif), lien → gabarit `/organisations/[slug]`. Aucun rich text (vérifié snapshot). **Mappers mutualisés dans `_shared`** (réutilisés par le hub B2C + pages offres à venir) : `mapOffers`/`mapOfferSummary`, `mapTestimonialItem`/`mapTestimonials`, `mapNumberedSteps`, `asAudience` ; `home.ts` refactoré pour déléguer le mapping témoignage. **Tests** : `org-hub.spec.ts` + extension `content-shared.spec.ts`. **67 tests web verts**, lint/typecheck/build (EXIT 0) verts
- [x] `/particuliers` Hub B2C — `docs/04-particuliers-hub.md` — page branchée sur `b2c_hub_page` + `faq_items` (scope=b2c) + `testimonials` (b2c, M2O unique `testimonial`), via l'endpoint caché `GET /api/content/b2c-hub`. 6 sections (accroche h1, **2 blocs « situation »** orientant vers les offres B2C, « comment je travaille », témoignage masqué si vide, **FAQ `FaqAccordion`**, CTA). Accent orange, ton empathique. **2 champs rich text** (`how_i_work_body` + FAQ `answer`) **assainis serveur** (sanitizer injecté). FAQ mutualisée dans `_shared` (`mapFaqItems`, réutilisée par la page Contact scope=contact). Liens situations = `cta_link` Directus avec repli sur les slugs d'offres documentés. **Tests** : `b2c-hub.spec.ts` + extension `content-shared.spec.ts`. **74 tests web verts**, lint/typecheck/build (EXIT 0) verts
- [x] Gabarit offre `/organisations/[slug]` + `/particuliers/[slug]` — `docs/05-offres-gabarit.md` — **gabarit unique** (DRY) : composant `OfferPage.vue` monté par 2 pages route fixant l'`audience` ; contenu via l'endpoint caché `GET /api/content/offer/:slug` (loader `content/offer.ts`, mappers purs). 9 sections (accroche h1 + fil d'Ariane, mission, « comment ça se passe »/« le format » selon public, outcomes, « pour qui », **investissement avec mention 293 B**, FAQ, témoignage masqué si vide, CTA → contact). `format_body` rich text **assaini serveur** (sanitizer injecté). **FAQ par scope** : map slug→scope (`audit`/`competences`/`managers`/`b2c`) + `general` transverse. **Garde d'audience** : slug d'un public servi sous l'autre route → 404 ; indisponibilité Directus → statut réel propagé (≠ 404). Hubs déplacés en `index.vue` (sinon route parent imbriquée). `mapTitledItems` mutualisé dans `_shared` (outcomes + convictions À propos refactorées). **Tests** : `offer.spec.ts` + extension `content-shared.spec.ts`. **86 tests web verts**, lint/typecheck/build (EXIT 0) verts. Contenu détaillé des offres = phase 2.
- [x] `/boutique` + `/boutique/[slug]` + `/boutique/confirmation` — `docs/06-boutique.md` — **catalogue** (`boutique/index.vue`) branché sur `/api/shop/products` (merge Directus published + Stripe actif), états chargement (squelette)/vide/erreur, `ProductCard` avec prix **formaté depuis Stripe** (util auto-importé `utils/price.ts`, jamais de prix en dur). **Fiche** (`boutique/[slug].vue`) : nouvel endpoint caché `GET /api/shop/products/:slug` (loader `loadProductDetail`/`mapProductDetail` dans `shop.ts`, **description rich text assainie serveur**, `game_details` label/value, galerie images, SEO + **JSON-LD `Product`**), **achat direct** (POST `/api/shop/checkout` mono-produit + `navigateTo` external), mention 293 B, 404 si dépublié/sans prix actif. **Confirmation** (`boutique/confirmation.vue`) lit `?session_id=`, accusé sobre, `noindex` (commande+email gérés par le webhook). Galerie sans lightbox + cross-sell = phase 2. **Activation éditoriale + vision évolutive** : nouveau singleton Directus **`shop_page`** (`shop_enabled` + `title`/`intro`/`empty_message` + SEO) → Eléonore active la boutique ET choisit les libellés **sans redéploiement** (générique, pas figé sur « jeux »). Endpoint caché `GET /api/content/shop` + composable partagé `useShopPage` (clé dédupliquée). Lien **« Boutique » dans header + footer conditionné** à `shop_enabled` ; catalogue désactivé → message « bientôt » ; fiche désactivée → 404. Schéma régénéré (bootstrap idempotent + snapshot + types, 18 collections) ; vérifié live (rôle API lecture lit `shop_page`). **Tests** : `price.spec.ts` (3) + `shop-page.spec.ts` (3) + extension `shop.spec.ts` (`mapGameDetails`, `mapProductDetail`). **96 tests web verts**, lint/typecheck/build (EXIT 0) verts
- [x] `/ressources` + `/ressources/[slug]` (fonctionne à 0 article) — `docs/07-ressources.md` — **index** (`ressources/index.vue`) branché sur `resources_page` + articles publiés + ressource vedette via l'endpoint caché `GET /api/content/resources` (loader `content/resources.ts`) : accroche, **ressource vedette** (`featured_resource` m2o → téléchargement direct OU gating newsletter selon `requires_email`), **filtres par groupe de catégorie** (client, dérivés des groupes présents, ordre documenté), grille `ArticleCard` avec **état vide propre à 0 article**. **Article** (`ressources/[slug].vue`) : endpoint caché `GET /api/content/article/:slug` (loader `content/article.ts`, `body` rich text **assaini serveur**, cover, catégorie, articles liés même catégorie, **JSON-LD `Article`**, breadcrumb, encart newsletter `CtaBlock`), 404 si introuvable. `ArticleSummary` étendu (`categorySlug`/`categoryGroup`), `mapArticle` (home) enrichi — **réutilisé** par index + liés (DRY). **Tests** : `resources.spec.ts` + `article.spec.ts`. **115 tests web verts**, lint/typecheck/build (EXIT 0) verts
- [x] `/newsletter` + `/newsletter/confirmation` — `docs/08-newsletter.md` — **inscription** (`newsletter/index.vue`) branchée sur `newsletter_page` via l'endpoint caché `GET /api/content/newsletter` (loader `content/newsletter.ts`) : nom (h1), `promise_body` **rich text assaini serveur**, `what_you_receive` (liste), `NewsletterForm` (déjà construit, double opt-in + Turnstile), cadeau de bienvenue (`welcome_gift_label` m2o resources → titre), aperçu (`sample_excerpt`/`sample_issue_label`). **Confirmation** (`newsletter/confirmation.vue`) : page d'état pure lisant `?status=success|already|expired|invalid` (renvoyé par la redirection 302 de `/api/newsletter/confirm`), retry → `/newsletter` si expiré/invalide, `noindex`. **Test** : `newsletter-content.spec.ts` (cadeau m2o résolu/non résolu, aperçu conditionné, promesse assainie, SEO). **107 tests web verts**, lint/typecheck/build (EXIT 0) verts
- [x] `/contact` (form + prise de RDV post-consentement) — `docs/09-contact.md` — page branchée sur `contact_page` + `faq_items` (scope=contact) + `site_settings`, via l'endpoint caché `GET /api/content/contact` (loader `content/contact.ts`). 5 sections : accroche h1, **2 voies** (`BookingEmbed` Cal.com chargé au consentement depuis `site_settings.booking_url` — masqué si pas d'URL ; `ContactForm` valibot partagé + Turnstile), « ce qui se passe ensuite » (`next_steps` + `response_time_note`), FAQ (`FaqAccordion`, rich text **assaini serveur**), coordonnées directes (email/LinkedIn/localisation). Mention RGPD discrète → `/confidentialite`. **Tests** : `contact.spec.ts` (RDV conditionné à booking_url, next_steps, FAQ assainie, coordonnées, SEO). **103 tests web verts**, lint/typecheck/build (EXIT 0) verts. ⚠️ embed Cal.com réel non vérifié live (cf. [[encre-humaine-etat]]).
- [x] `/mentions-legales` · `/cgv` · `/confidentialite` · `/cgu` — `docs/10-legal.md` — **gabarit mutualisé** `LegalPage.vue` (les 4 pages route fixent le slug) branché sur `legal_documents` (par slug) via l'endpoint caché `GET /api/content/legal/:slug` (loader `content/legal.ts`). `body` rich text **assaini serveur**, **table des matières auto** (h2 ancrés, ids uniques dédoublonnés, `buildToc` ; affichée si ≥3 entrées ; `scroll-margin-top` global sur `.rich-text h2/h3`), date de mise à jour, 404 si introuvable. Lien CGU ajouté au footer. **Tests** : `legal.spec.ts` (slugify, buildToc, mapper). **120 tests web verts**, lint/typecheck/build (EXIT 0) verts. ⚠️ **CONTENU** = à saisir dans Directus `legal_documents` (slugs `mentions-legales`/`cgv`/`cgu`/`confidentialite`) : Franck a fourni les textes mais ils nécessitent réconciliation (domaine, providers réels Cal.com/Resend/Umami/Hetzner, placeholders SIRET/hébergeur/médiateur) — cf. [[encre-humaine-blockers]].

### Sécurité & conformité — `docs/06-security.md`
- [x] Rate-limiting endpoints publics (IP réelle via `CF-Connecting-IP`) — `server/utils/rate-limit.ts` (fenêtre glissante en mémoire, cœur testable à horloge injectée) + `client-ip.ts` (CF-Connecting-IP → X-Forwarded-For → socket). Appliqué sur `/api/contact` & `/api/newsletter/subscribe` (5/min/IP) et `/api/shop/checkout` (10/min/IP). Test `test/rate-limit.spec.ts`. NB : store mémoire = mono-instance (phase 1)
- [x] Turnstile serveur (siteverify) + honeypot — `server/utils/turnstile.ts` (`verifyTurnstile` siteverify, **fail-closed**) sur contact + newsletter ; honeypot `website` court-circuité côté serveur (faux succès)
- [x] CSP stricte + en-têtes sécurité — **CSP à nonce** via module **nuxt-security 2.6.0** (`apps/web/nuxt.config.ts §security`) : `script-src` avec `'nonce-{{nonce}}'` (PAS de `'unsafe-inline'`) car Nuxt émet un `<script>` inline de config qui doit porter un nonce par requête — sinon l'app casse sous CSP stricte (vérifié : sans nonce le script serait bloqué). `style-src 'unsafe-inline'` conservé (styles SSR + attributs `style=` non nonçables). Allow-list tiers Stripe/Cal.com/Turnstile/Umami/R2/Directus + `frame-ancestors`/`base-uri`/`form-action`/`object-src 'none'`/`upgrade-insecure-requests`. En-têtes HSTS/X-Frame-Options/Referrer-Policy/Permissions-Policy/COOP posés par le module (COEP off : casserait les iframes Cal/Stripe). Middlewares mutateurs **désactivés** (`rateLimiter`/`xssValidator`/`requestSizeLimiter`/`corsHandler`) pour ne pas casser le webhook Stripe (body brut signé) ni les formulaires + rate-limit/anti-bot déjà maison. **CSP retirée du Caddyfile** pour le site public (le module l'owne ; évite les doublons) ; Caddy garde les en-têtes pour `cms.`/`stats.` (hors Nuxt). **Vérifié en prod-like** : en-tête CSP correct + `<script>` inline noncé (+ SRI sur le bundle local), Caddyfile s'adapte/formate sans erreur. ⚠️ À valider en navigateur réel au déploiement (Stripe/Cal.com/Turnstile/Umami live).
- [~] Bandeau de consentement maison (gate embeds tiers) — `ConsentBanner` + `useConsent` + gate `BookingEmbed` (Cal.com) faits ; Umami exempté (cookieless). Reste : gate des éventuels autres embeds tiers.
- [x] Umami cookieless intégré — script injecté côté front via `plugins/analytics.client.ts` (chargé **sans consentement**, cookieless) + util pur `utils/analytics.ts` (`analyticsScript` → descripteur ou `null` si non configuré, **testé** `test/analytics.spec.ts`). 2 vars **publiques agnostiques** (cf. [[encre-humaine-nommage-agnostique]]) `ANALYTICS_SCRIPT_URL` + `ANALYTICS_WEBSITE_ID` (env schema **optionnelles**, runtimeConfig public, `.env.example`). CSP Caddy autorisait déjà `stats.encrehumaine.fr` (script-src/connect-src). ⚠️ **website id à créer dans le dashboard `stats.` au déploiement** puis renseigner `ANALYTICS_WEBSITE_ID` (vide → aucun script injecté).

### Déploiement — `docs/07-deploy.md`
- [x] CI Forgejo (lint + typecheck + tests bloquants → build → deploy) — 2 workflows `.forgejo/workflows/` : **`ci.yml`** (push `main` + PR → lint/typecheck/tests/build dans `node:26.0.0-bookworm-slim`, runner Docker dédié) + **`deploy.yml`** (tag `v*` ou `workflow_dispatch` → SSH Hetzner → `git checkout` + `docker compose up -d --build --wait`, **build sur le serveur**, `.env` maintenu à la main sur l'hôte). Tests PGlite fiabilisés (`hookTimeout: 60s`). README de setup (`.forgejo/workflows/README.md`) : secrets Forgejo `DEPLOY_*`, pré-requis serveur, durcissement firewall CF. ⚠️ **reste côté Franck** : enregistrer le runner (label `docker`), créer les secrets `DEPLOY_SSH_KEY`/`KNOWN_HOSTS`/`USER`/`HOST`/`PATH`, cloner le dépôt + créer `infra/env/.env` sur Hetzner. YAML validé + commandes éprouvées en local ; exécution Forgejo réelle non testée ici.
- [x] Miroir GitHub — en place (configuré depuis Forgejo).
- [ ] DNS Cloudflare (`@`, `cms`, `stats`) + token DNS scope
- [ ] Restauration d'un dump testée avant prod

---

## Phase 2+ (rappel, hors périmètre immédiat)
- Contenu détaillé des pages offres · rédaction articles · diffusion newsletter (broadcasts Resend)
- Vue Commandes (fulfillment) · inventaire Stripe automatisé · Europe/expédition élargie
- Bascule TVA (`VAT_ENABLED`) si dépassement de seuil — sans migration (ADR #4)

# Strict, reproducible installs (politique de dépendances — docs/CLAUDE.md)
engine-strict=true
# Évite l'élévation accidentelle de dépendances non déclarées
auto-install-peers=true
strict-peer-dependencies=false
